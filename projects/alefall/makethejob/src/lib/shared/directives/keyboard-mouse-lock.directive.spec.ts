import { KeyboardMouseLockDirective } from './keyboard-mouse-lock.directive';

describe('KeyboardMouseLockDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyboardMouseLockDirective();
    expect(directive).toBeTruthy();
  });
});
