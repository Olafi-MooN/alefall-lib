import { KeyboarkMouseLockDirective } from './keyboark-mouse-lock.directive';

describe('KeyboarkMouseLockDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyboarkMouseLockDirective();
    expect(directive).toBeTruthy();
  });
});
